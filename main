local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer

local function GetSafeGui()
    if gethui then return gethui() end
    if CoreGui then return CoreGui end
    return LocalPlayer:WaitForChild("PlayerGui")
end

local parentGui = GetSafeGui()

local COLORS = {
    BG = Color3.fromRGB(13, 17, 23),
    SIDEBAR = Color3.fromRGB(10, 12, 16),
    STROKE = Color3.fromRGB(34, 211, 238), -- Ciano
    TEXT = Color3.fromRGB(241, 245, 249),
    ACCENT = Color3.fromRGB(34, 211, 238),
    CARD_BG = Color3.fromRGB(17, 22, 28),
    TOGGLE_ON = Color3.fromRGB(34, 211, 238),
    TOGGLE_OFF = Color3.fromRGB(45, 55, 72),
    HOVER_RED = Color3.fromRGB(255, 60, 60)
}

if parentGui:FindFirstChild("ChinelaoHub_Final_V2") then
    parentGui:FindFirstChild("ChinelaoHub_Final_V2"):Destroy()
end

local sg = Instance.new("ScreenGui")
sg.Name = "ChinelaoHub_Final_V2"
sg.ResetOnSpawn = false
sg.Parent = parentGui

-- [[ ARRASTE ]] --
local function Drag(gui, target)
    local target = target or gui
    local dragging, dragInput, dragStart, startPos
    gui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; dragStart = input.Position; startPos = target.Position
        end
    end)
    gui.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            target.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
    end)
end

-- [[ BOTÃO MINIMIZADO COM A SUA IMAGEM ]] --
local OpenBtn = Instance.new("Frame", sg)
OpenBtn.Size = UDim2.fromOffset(65, 65)
OpenBtn.Position = UDim2.new(0, 50, 0.5, -32)
OpenBtn.BackgroundColor3 = COLORS.BG
OpenBtn.Visible = false
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(0, 15)
Instance.new("UIStroke", OpenBtn).Color = COLORS.ACCENT

local LogoImg = Instance.new("ImageLabel", OpenBtn)
LogoImg.Size = UDim2.new(1, 0, 1, 0)
LogoImg.BackgroundTransparency = 1
-- USANDO O LINK DIRETO DA IMAGEM GERADA:
LogoImg.Image = "https://googleusercontent.com/image_generation_content/9" 
Instance.new("UICorner", LogoImg).CornerRadius = UDim.new(0, 15)

local OpenTrigger = Instance.new("TextButton", OpenBtn)
OpenTrigger.Size = UDim2.new(1, 0, 1, 0); OpenTrigger.BackgroundTransparency = 1; OpenTrigger.Text = ""
Drag(OpenTrigger, OpenBtn)

-- [[ FRAME PRINCIPAL ]] --
local Main = Instance.new("Frame", sg)
Main.AnchorPoint = Vector2.new(0.5, 0.5); Main.Size = UDim2.fromOffset(720, 480)
Main.Position = UDim2.new(0.5, 0, 0.5, 0); Main.BackgroundColor3 = COLORS.BG
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 12)
Instance.new("UIStroke", Main).Color = COLORS.STROKE
Drag(Main)

-- BARRA SUPERIOR
local TopBar = Instance.new("Frame", Main)
TopBar.Size = UDim2.new(1, 0, 0, 55); TopBar.BackgroundTransparency = 1

local Title = Instance.new("TextLabel", TopBar)
Title.Size = UDim2.new(0, 300, 1, 0); Title.Position = UDim2.new(0, 25, 0, 0); Title.BackgroundTransparency = 1
Title.Text = "CHINELÃO HUB"; Title.TextColor3 = COLORS.TEXT; Title.Font = Enum.Font.GothamBold; Title.TextSize = 18; Title.TextXAlignment = 0

local Controls = Instance.new("Frame", TopBar)
Controls.Size = UDim2.new(0, 100, 1, 0); Controls.Position = UDim2.new(1, -110, 0, 0); Controls.BackgroundTransparency = 1

local function createControlBtn(txt, pos, hoverColor, callback)
    local b = Instance.new("TextButton", Controls)
    b.Size = UDim2.fromOffset(35, 35); b.Position = UDim2.new(0, pos, 0.5, -17)
    b.BackgroundColor3 = Color3.fromRGB(25, 30, 40); b.Text = txt; b.TextColor3 = COLORS.TEXT; b.Font = Enum.Font.GothamBold; b.TextSize = 18
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
    b.MouseEnter:Connect(function() b.BackgroundColor3 = hoverColor end)
    b.MouseLeave:Connect(function() b.BackgroundColor3 = Color3.fromRGB(25, 30, 40) end)
    b.MouseButton1Click:Connect(callback)
end

createControlBtn("-", 0, COLORS.ACCENT, function() Main.Visible = false; OpenBtn.Visible = true end)
createControlBtn("X", 45, COLORS.HOVER_RED, function() sg:Destroy() end)

OpenTrigger.MouseButton1Click:Connect(function() OpenBtn.Visible = false; Main.Visible = true end)

-- SIDEBAR
local Sidebar = Instance.new("Frame", Main)
Sidebar.Size = UDim2.new(0, 190, 1, -130); Sidebar.Position = UDim2.new(0, 0, 0, 65); Sidebar.BackgroundTransparency = 0.5; Sidebar.BackgroundColor3 = COLORS.SIDEBAR
Instance.new("UIListLayout", Sidebar).HorizontalAlignment = Enum.HorizontalAlignment.Center

-- PERFIL
local UserProfile = Instance.new("Frame", Main)
UserProfile.Size = UDim2.new(0, 180, 0, 65); UserProfile.Position = UDim2.new(0, 5, 1, -75); UserProfile.BackgroundTransparency = 1

local pfp = Instance.new("ImageLabel", UserProfile)
pfp.Size = UDim2.fromOffset(50, 50); pfp.Position = UDim2.new(0, 12, 0.5, -25)
pfp.Image = "rbxthumb://type=AvatarHeadShot&id=" .. LocalPlayer.UserId .. "&w=150&h=150"
Instance.new("UICorner", pfp).CornerRadius = UDim.new(1, 0)

local NameLbl = Instance.new("TextLabel", UserProfile)
NameLbl.Size = UDim2.new(1, -75, 0, 20); NameLbl.Position = UDim2.new(0, 70, 0.2, 0); NameLbl.BackgroundTransparency = 1
NameLbl.Text = LocalPlayer.DisplayName; NameLbl.TextColor3 = COLORS.TEXT; NameLbl.Font = Enum.Font.GothamBold; NameLbl.TextSize = 14; NameLbl.TextXAlignment = 0

local PremLbl = Instance.new("TextLabel", UserProfile)
PremLbl.Size = UDim2.new(1, -75, 0, 15); PremLbl.Position = UDim2.new(0, 70, 0.55, 0); PremLbl.BackgroundTransparency = 1
PremLbl.Text = "Premium User"; PremLbl.TextColor3 = COLORS.ACCENT; PremLbl.Font = Enum.Font.GothamMedium; PremLbl.TextSize = 12; PremLbl.TextXAlignment = 0

-- CONTAINER
local Container = Instance.new("Frame", Main)
Container.Size = UDim2.new(1, -230, 1, -90); Container.Position = UDim2.new(0, 210, 0, 75); Container.BackgroundTransparency = 1

local pages = {}
local function createPage(name)
    local p = Instance.new("ScrollingFrame", Container); p.Size = UDim2.new(1, 0, 1, 0); p.BackgroundTransparency = 1; p.Visible = false; p.ScrollBarThickness = 3
    Instance.new("UIListLayout", p).Padding = UDim.new(0, 15); pages[name] = p; return p
end

local pPlayer = createPage("PLAYER"); pPlayer.Visible = true
local pFly = createPage("FLY"); local pServer = createPage("SERVERS")

local function createTab(name)
    local b = Instance.new("TextButton", Sidebar)
    b.Size = UDim2.new(0, 170, 0, 45); b.BackgroundColor3 = Color3.fromRGB(25, 30, 40); b.Text = name; b.TextColor3 = COLORS.TEXT; b.Font = Enum.Font.GothamBold; b.TextSize = 14
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
    b.MouseButton1Click:Connect(function() for _, v in pairs(pages) do v.Visible = false end; pages[name].Visible = true end)
end
createTab("PLAYER"); createTab("FLY"); createTab("SERVERS")

-- TOGGLE
local function createToggle(parent, text, callback)
    local card = Instance.new("Frame", parent); card.Size = UDim2.new(1, -10, 0, 60); card.BackgroundColor3 = COLORS.CARD_BG; Instance.new("UICorner", card).CornerRadius = UDim.new(0, 10)
    local l = Instance.new("TextLabel", card); l.Text = text; l.Size = UDim2.new(1, -80, 1, 0); l.Position = UDim2.new(0, 20, 0, 0); l.TextColor3 = COLORS.TEXT; l.Font = Enum.Font.GothamSemibold; l.TextSize = 15; l.BackgroundTransparency = 1; l.TextXAlignment = 0
    local btn = Instance.new("TextButton", card); btn.Size = UDim2.fromOffset(50, 26); btn.Position = UDim2.new(1, -70, 0.5, -13); btn.BackgroundColor3 = COLORS.TOGGLE_OFF; btn.Text = ""; Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)
    local dot = Instance.new("Frame", btn); dot.Size = UDim2.fromOffset(20, 20); dot.Position = UDim2.new(0, 3, 0.5, -10); dot.BackgroundColor3 = Color3.new(1,1,1); Instance.new("UICorner", dot).CornerRadius = UDim.new(1,0)
    local act = false
    btn.MouseButton1Click:Connect(function() act = not act; dot.Position = act and UDim2.new(1, -23, 0.5, -10) or UDim2.new(0, 3, 0.5, -10); btn.BackgroundColor3 = act and COLORS.TOGGLE_ON or COLORS.TOGGLE_OFF; callback(act) end)
end

createToggle(pPlayer, "Auto Farm", function(v) print("Farm:", v) end)
createToggle(pFly, "Fly V3", function(v) if v then loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-FLY-GUI-V3-19840"))() end end)
